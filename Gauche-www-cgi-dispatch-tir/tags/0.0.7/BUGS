発見され、まだ修正されていないバグは、このファイルに記録します。
（つまり、このファイルがバグトラッキングシステムです）
バグには、適当な識別用番号を連番で振ります。
バグが修正されても、このファイルからは消さないようにします。
（代わりに「yyyymmdd修正済」等と追記します）

----

* 00000001 : cgi-entry->url等が、関数ではなくマクロなので、applyに渡せず、不便

- しかし、(current-module)の仕様上、どうしようもない……。
- 何かいい方法を考える事。
-- 隠蔽するのは、もう諦めて、cgi-entry->urlマクロが呼んでいる実体の関数の、
   module&cgi-entry->urlをユーザに見せて、使わせるのはどうか。
--- ちょっと、みっともないような気が少しする。
-- モジュール空間でエントリテーブルを分けるのは諦める？
--- コレは、分けられないと、きっと不便。
-- 普通に、インスタンスを生成して、cgi-entry->url等を呼ぶ際に、
   インスタンスも指定するようにする？
--- やっぱり、簡潔に記述できるのは捨て難い。
- 今のところ、ユーザにmodule&cgi-entry->urlを見せて、
  applyを使いたい時は、明示的に、
  (apply module&cgi-entry->url (current-module) ...)のように
  呼ばせるようにする方向で考える。
-- とりあえず、この方向で修正してみた。


# 20051030追加/未修正/未着手
# 20051101対応/修正済

----

* 00000000 : 開始

- 今のところ、何も見つかっていない

# 20051030追加/未修正/未着手

----

$Id$
